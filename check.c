#include<stdio.h>

int main(){
    int a;
    char b;
    unsigned long long int k;
    long double c;
     printf("size of variable is %d\n", sizeof(int) );
     printf("size of variable is %d\n", sizeof(char) );
     printf("size of variable is %d\n", sizeof(unsigned long long int) );
     printf("size of variable is %d\n", sizeof(long double) );

    return 0;
}